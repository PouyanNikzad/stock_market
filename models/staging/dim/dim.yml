version: 2

sources:
  - name: dim
    database: DATA_ENG_DBT
    schema: TEST
    loader: snowflake
    description: >
      Raw tables loaded from source systems via ingestion pipelines.
      These tables are not created by dbt.

    tables:
      - name: DIM_TEST
        description: Raw DIM_TEST table
        config:
          loaded_at_field: LOAD_DATE  